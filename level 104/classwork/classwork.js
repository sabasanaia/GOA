// 1)კომენტარის სახით ახსენით შემდეგი მეთოდები და კუთვნილებები: split / join / splice / concat / length pop / push / slice / shift / unshift / flat
// split - splits an string into a list 
// join - it joins a lists indexes and turns it into a list
// splice -
// concat - connects 2 lists to make a new one whit both lists indexes
// lenght - counts how many indexes are in the list
// pop - deletes an index of your choosing
// push - adds a index into the end of the list
// slice - 
// shift - removes the last index of the list
// unshift - removes an index and adds a new index
// flat - if theres a list in a list it will remove the list that was in the list but adds the removed lists indexes into the list that it was in
// 2)split – Use split() to separate the string "John-Doe-1990-Engineer" into an array and extract the job title.
console.log("John-Doe-1990-Engineer".split())
// 3)split – Given "2025/09/05 13:45", split the date and time parts into separate arrays.

// 4)join – Join the array ["Learn", "JS", "Step", "by", "Step"] into a sentence with spaces.
console.log(["Learn", "JS", "Step", "by", "Step"].join(" "))
// 5)join – Given the array ["2025", "09", "05"], join it into a date string with slashes (/).

// 6)splice – Use splice() to replace 2 elements in ["red", "green", "blue", "yellow"] starting from index 1 with "orange".

// 7)splice – From the array [1, 2, 3, 4, 5], remove the middle value and insert "X" and "Y" in its place.

// 8)concat – Concatenate ["Jan", "Feb"] with ["Mar", "Apr"] and add "May" at the end using concat().

// 9)concat – Merge ["frontend"] with ["backend", "fullstack"] and store the result in a new array.

// 10)length – Get the length of the array created by splitting "one two three four" by space.

// 11)length – Check the length of the result when you slice() the array [0,1,2,3,4,5,6] from index 2 to 5.

// 12)pop – Use pop() on [100, 200, 300, 400], then add the removed value to a new array.

// 13)pop – Repeatedly pop() from ["a", "b", "c", "d"] until it's empty, and count the number of pops.

// 14)push – Use push() to add the result of "Hello".split("") into an array.